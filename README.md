# EchoMind

EchoMind is organized as a single repository containing the backend service and frontend application.

## Directories

- `backend/` — Python backend and supporting services
- `frontend/` — Vue/Vite frontend application

See the README files in each directory for component-specific setup instructions.
